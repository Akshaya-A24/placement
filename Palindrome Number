class Solution {
    public boolean isPalindrome(int n) {
       if(n<0) return false;
        int temp =n;
        int pal =0 ;
        while(n != 0){
            pal = pal*10 + (n%10);
            n = n / 10;
        }
return pal == temp;
    }
}
